spring:
  datasource:
    url: jdbc:mysql://e202-mysql-db.cxwweg8egtc4.ap-southeast-2.rds.amazonaws.com:3306/E202
    username: "admin"
    password: "ssafy1234"
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: "galaxyaippt@gmail.com"
    password: "eoeuezuplqwiikel"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # Spring Data Redis 설정 (권장)
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password:
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 60s

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.backend.backend.domain.*.dto
  configuration:
    map-underscore-to-camel-case: true

app:
  auth:
    mail:
      from: "galaxyaippt@gmail.com"
    otp:
      length: 4
      expire: 3m
      verification-token-expire: 10m

jwt:
  token:
    secret: your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
    access-expiration: 3600000
    refresh-expiration: 86400000